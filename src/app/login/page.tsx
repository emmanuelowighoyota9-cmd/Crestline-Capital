'use client';

import Link from 'next/link';
import { useState } from 'react';
import { ShieldCheckIcon } from '../components/Icons';

export default function LoginPage() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent) { e.preventDefault(); setError(''); setLoading(true);
    try { const res = await fetch('/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) }); const data = await res.json(); if (!res.ok) throw new Error(data.error || 'Login failed'); if (data.user.role === 'admin') { window.location.href = '/admin/dashboard'; } else { window.location.href = '/dashboard'; } } catch (err: any) { setError(err.message); } finally { setLoading(false); }
  }

  return (<div className="min-h-screen flex bg-slate-950"><div className="hidden lg:flex lg:w-1/2 relative overflow-hidden"><div className="absolute inset-0 bg-grid opacity-20" /><div className="absolute top-1/3 left-1/2 -translate-x-1/2 w-[600px] h-[600px] rounded-full bg-gradient-to-br from-crestline-600/30 to-transparent blur-3xl" /><div className="relative flex flex-col justify-center px-16"><Link href="/" className="flex items-center gap-2.5 mb-12"><div className="flex h-8 w-8 items-center justify-center rounded-lg bg-gradient-to-br from-crestline-500 to-crestline-700"><span className="text-sm font-bold text-white">C</span></div><span className="text-lg font-bold text-white">Crestline<span className="text-crestline-400">Capital</span></span></Link><h2 className="text-4xl font-bold text-white">Welcome back to <span className="text-gradient">Crestline Capital</span></h2><p className="mt-4 text-lg text-slate-400">Access your accounts, investments, and financial tools.</p><div className="mt-12 grid gap-4">{[{ title: '256-bit Encryption', desc: 'Military-grade security for every session' },{ title: 'Instant Access', desc: 'View all accounts from a single dashboard' },{ title: '24/7 Monitoring', desc: 'Real-time fraud detection and alerts' }].map(i => (<div key={i.title} className="flex items-center gap-3"><ShieldCheckIcon className="h-5 w-5 text-crestline-400 flex-shrink-0" /><div><p className="text-sm font-medium text-white">{i.title}</p><p className="text-xs text-slate-500">{i.desc}</p></div></div>))}</div></div></div><div className="flex flex-1 items-center justify-center px-4 sm:px-6 lg:px-8"><div className="w-full max-w-md"><div className="lg:hidden mb-8 text-center"><Link href="/" className="inline-flex items-center gap-2.5"><div className="flex h-8 w-8 items-center justify-center rounded-lg bg-gradient-to-br from-crestline-500 to-crestline-700"><span className="text-sm font-bold text-white">C</span></div><span className="text-lg font-bold text-white">Crestline<span className="text-crestline-400">Capital</span></span></Link></div><div className="rounded-2xl border border-white/5 bg-slate-900/50 p-8"><h1 className="text-2xl font-bold text-white">Sign In</h1><p className="mt-1 text-sm text-slate-400">Enter your credentials to access your account</p><form onSubmit={handleSubmit} className="mt-8 space-y-5">{error && <div className="rounded-lg border border-red-500/20 bg-red-500/5 px-4 py-3 text-sm text-red-400">{error}</div>}<div><label className="block text-sm font-medium text-slate-300">Email address</label><input type="email" required value={email} onChange={e => setEmail(e.target.value)} className="mt-1.5 block w-full rounded-lg border border-white/10 bg-slate-800/50 px-4 py-2.5 text-white placeholder-slate-500 focus:border-crestline-500 focus:outline-none" placeholder="you@example.com" /></div><div><label className="block text-sm font-medium text-slate-300">Password</label><input type="password" required value={password} onChange={e => setPassword(e.target.value)} className="mt-1.5 block w-full rounded-lg border border-white/10 bg-slate-800/50 px-4 py-2.5 text-white placeholder-slate-500 focus:border-crestline-500 focus:outline-none" placeholder="........" /></div><button type="submit" disabled={loading} className="w-full rounded-xl bg-crestline-600 px-4 py-3 text-sm font-semibold text-white shadow-lg shadow-crestline-500/25 hover:bg-crestline-500 disabled:opacity-50">{loading ? 'Signing in...' : 'Sign In'}</button></form><p className="mt-6 text-center text-sm text-slate-400">Don't have an account? <Link href="/register" className="font-medium text-crestline-400 hover:text-crestline-300">Create one</Link></p></div><p className="mt-6 text-center text-xs text-slate-500">Protected by 256-bit AES encryption.</p></div></div></div>);
}
